---
title: rollback
---
import { Aside, Tabs, TabItem, LinkButton } from '@astrojs/starlight/components';

### 概念

<LinkButton href="/ja/docs/hooks/concepts/introduction/">はじめに</LinkButton>
<LinkButton href="/ja/docs/hooks/concepts/execution-metadata">実行メタデータ</LinkButton>

### 動作

Hookの実行を拒否で終了します。

* トランザクションメタデータに戻り文字列と戻りコードを記録します。
* すべての状態変更を破棄します。
* すべての `emit()` トランザクションを破棄します。
* 発信トランザクションの継続を許可しません。

<Aside type="caution">
  発信トランザクションは <code>tecHOOK_REJECTED</code> で失敗し、手数料が請求されます。参照:実行順序。
</Aside>

### 定義

<Tabs>
<TabItem label="C">
```c
int64_t rollback (
    uint32_t read_ptr,
    uint32_t read_len,
    uint64_t error_code
);
```
</TabItem>
<TabItem label="JavaScript">
```javascript
function rollback(error_msg: string, error_code: number): number
```
</TabItem>
</Tabs>

### 例

<Tabs>
<TabItem label="C">
```c
rollback("Rejected!", 9, 100);
```
</TabItem>
<TabItem label="JavaScript">
```javascript
rollback('Rejected!', 100)
```
</TabItem>
</Tabs>

### パラメーター

<Tabs>
<TabItem label="C">

<table><thead><tr><th>名前</th><th>型</th><th width="202">説明</th></tr></thead><tbody><tr><td>read_ptr</td><td>uint32_t</td><td>実行メタデータに格納される戻り文字列へのポインターです。<br />これは、Hook開発者がreject時に返したい任意の文字列です。<em>nullにしてもよい。</em></td></tr><tr><td>read_len</td><td>uint32_t</td><td>戻り文字列の長さです。最大32です。<em>nullにしてもよい。</em></td></tr><tr><td>error_code</td><td>uint64_t</td><td>このHook固有の戻りコードで、実行メタデータに格納されます。<br /><br />*nixシステム上のアプリケーションの戻りコードに似ています。慣例上、成功以外は0ではありません。</td></tr></tbody></table>
</TabItem>
<TabItem label="JavaScript">

<table><thead><tr><th>名前</th><th>型</th><th width="202">説明</th></tr></thead><tbody><tr><td>error_msg</td><td>string</td><td>実行メタデータに格納される文字列です。<br />これは、Hook開発者がreject時に返したい任意の文字列です。<em>nullにしてもよい。</em></td></tr><tr><td>error_code</td><td>number</td><td>このHook固有の戻りコードで、実行メタデータに格納されます。<br /><br />*nixシステム上のアプリケーションの戻りコードに似ています。慣例上、成功以外は0ではありません。</td></tr></tbody></table>
</TabItem>
</Tabs>

### 戻りコード

<Tabs>
<TabItem label="C">
<table><thead><tr><th width="123">型</th><th>説明</th></tr></thead><tbody><tr><td>int64_t</td><td>RollbackはHookを終了するため、呼び出し元には値は返されません。慣例としてすべてのHook APIは <code>int64_t</code> を返しますが、この場合は何も返されません。</td></tr></tbody></table>
</TabItem>
<TabItem label="JavaScript">
<table><thead><tr><th width="123">型</th><th>説明</th></tr></thead><tbody><tr><td>number</td><td>RollbackはHookを終了するため、呼び出し元には値は返されません。慣例としてすべてのHook APIは <code>number</code> を返しますが、この場合は何も返されません。</td></tr></tbody></table>
</TabItem>
</Tabs>

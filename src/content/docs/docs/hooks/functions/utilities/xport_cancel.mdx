---
title: xport_cancel
description: >-
  Cancel a shadow ticket from within a hook, freeing the associated reserve.
---
import { Aside } from '@astrojs/starlight/components';

### Behaviour

- Cancels a shadow ticket created by a successful Export transaction, including one that originated from a previous `xport()` call.
- The shadow ticket is deleted and its reserve is freed.
- It does not revoke validator signature shares already published to the peer network.

<Aside type="caution">
A destination transaction may still execute after cancellation. Its returned XPOP cannot trigger the callback while the latch is absent. Cancel only under an explicit recovery policy that accounts for public signatures, delayed target submission, and callback replay; otherwise import the target-chain proof to consume the shadow ticket.
</Aside>

### Definition

```c
int64_t xport_cancel (
    uint32_t ticket_seq
);
```

### Example

```c
int64_t result = xport_cancel(2);  // cancel shadow ticket for TicketSequence 2
if (result < 0)
    rollback(SBUF("Cancel failed"), 1);
```

### Parameters

| Name       | Type     | Description                                                    |
| ---------- | -------- | -------------------------------------------------------------- |
| ticket_seq | uint32_t | The ticket sequence of the shadow ticket to cancel.             |

### Return Code

| Type    | Description                                                                                   |
| ------- | --------------------------------------------------------------------------------------------- |
| int64_t | The cancelled ticket sequence on success. Check for `result >= 0`, not `result == 1`. If negative, an error: `DOESNT_EXIST` — no shadow ticket found for the specified ticket sequence on this account. |

### Related

- [Cross-Chain Export overview](/docs/features/export)
- [Export transaction reference](/docs/protocol-reference/transactions/transaction-types/export)
- [xport()](/docs/hooks/functions/utilities/xport)
